# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.110.0
python-dotenv>=1.0.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# ── HTTP client ────────────────────────────────────────────────────────────
httpx>=0.27.0

# ── 3D printer ────────────────────────────────────────────────────────────
bambulabs-api>=0.4.0        # LAN MQTT status polling
bambu-lab-cloud-api>=1.0.5  # Cloud print dispatch (auto-start, no touchscreen)

# ── Computer vision (Phase 1 — calibration, Phase 4 — scanning) ───────────
opencv-python>=4.9.0
numpy>=1.26.0
imutils>=0.5.4

# ── File watching (Phase 6 — SMB gallery watcher) ─────────────────────────
watchdog>=4.0.0

# ── Image handling (Phase 6 — HEIC conversion) ────────────────────────────
Pillow>=10.3.0
pillow-heif>=0.16.0

# ── WebSocket support (included via uvicorn[standard] but explicit) ────────
websockets>=12.0

# ── Home Assistant MQTT bridge (Phase 3) ──────────────────────────────────
paho-mqtt>=2.0.0

# ── AI / Vision (Phase 4 — object identification via Gemini) ──────────────
google-generativeai>=0.8.0

# ── Voice bridge (Phase 8) ────────────────────────────────────────────────
wyoming>=1.5.0              # Wyoming protocol — STT/TTS TCP servers for HA integration
onnxruntime>=1.17.0         # ONNX inference runtime for openWakeWord (x86_64 native)
sounddevice>=0.4.6          # Audio I/O — microphone capture and speaker playback
# NOTE: openwakeword must be installed separately due to tflite-runtime incompatibility
#       with Python 3.12+. Run AFTER pip install -r requirements.txt:
#         venv/bin/pip install openwakeword --no-deps
